import React, { useState } from "react";
import SearchBar from "./SearchBar";

function Header() {
    return (
        <header>
            <SearchBar />
        </header>
    );
}

export default Header;
